---
export interface Props {
    sectionId: string;
}

const { sectionId } = Astro.props;
---

<div class="snap-start scroll-mb-40">
    <section id={sectionId} class="section w-full min-h-[100lvh] flex flex-col items-center">
        <slot />
    </section>
</div>